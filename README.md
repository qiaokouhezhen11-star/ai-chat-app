# AIチャットアプリ

ChatGPTライクなUIで、AIとの会話、ストリーミング応答、ファイル参照ができるチャットアプリです。  
`.txt` / `.md` ファイルをアップロードし、その内容をもとにAIへ質問できる機能を実装しています。

転職用ポートフォリオとして、**生成AIアプリの基本機能** に加えて、**実務を意識したファイル連携機能** まで実装しました。

---

## Repository

- GitHub: https://github.com/qiaokouhezhen11-star/ai-chat-app

---

## 制作背景

生成AIアプリ開発の学習と、転職用ポートフォリオ作成を目的に開発しました。  
単なるチャット機能だけではなく、**ファイル内容をもとにAIへ質問できる実用的な体験** を意識して設計しています。

---

## 主な機能

- AIチャットの送受信
- ストリーミング応答表示
- 回答生成の停止
- 再送機能
- 会話履歴のローカル保存
- モデル切り替え
- オフライン検知
- 文字数制限表示
- `.txt` / `.md` ファイルアップロード
- アップロードファイル内容をもとにした回答
- ファイル名 / 拡張子 / サイズ表示
- ファイル付き送信履歴の表示
- PDF未対応時の安全な制御
- 未対応ファイル形式 / サイズ超過時の画面内エラー表示

---

## 対応ファイル形式

### 対応済み
- `.txt`
- `.md`

### 未対応
- `.pdf`

※ PDFは現時点では未対応ですが、フロント側・サーバー側の両方で安全に制御しています。

---

## 技術スタック

### フロントエンド
- React
- Vite
- JavaScript
- React Markdown
- remark-gfm
- lucide-react

### バックエンド
- API Route
- OpenAI API

### その他
- localStorage
- FormData
- ReadableStream
- AbortController

---

## 工夫したポイント

### 1. ファイル連携を自然にチャット体験へ組み込んだ
`.txt` / `.md` をアップロードし、その内容をAI回答に反映できるようにしました。  
また、**どのファイルを使って送信したかが履歴に残る設計** にしているため、会話をあとから見返したときにも分かりやすくしています。

### 2. フロントとサーバーの両方でファイルバリデーションを実施
未対応形式やPDFについて、**フロント側だけでなくサーバー側でも制御** しています。  
これにより、UI上の誤操作だけでなく、不正な送信に対しても安全な構成にしています。

### 3. エラー表示を画面内UIに統一
初期段階では `alert` を使っていましたが、最終的には **画面内にエラーメッセージを表示する方式** に変更しました。  
これにより、ユーザー体験とアプリの完成度を高めています。

### 4. 再送処理の安定化
再送時に `setTimeout` に依存しないように整理し、**送る本文を直接渡せる構成** に変更しました。  
Reactのstate反映タイミングに依存しにくい、より安定した実装を意識しています。

### 5. 共通定数・表示ロジック・ファイル入力処理の分離
ファイル関連定数や表示用ヘルパー、ファイル入力処理を別ファイルへ切り出し、変更や拡張に強い構成に整理しました。

---

## ディレクトリ構成（一部）

```txt
apps/web/src/
├─ app/
│  ├─ api/
│  │  └─ chat/
│  │     └─ route.js
│  └─ chat/
│     └─ page.jsx
├─ constants/
│  └─ fileUpload.js
├─ hooks/
│  └─ useChatFileInput.js
├─ utils/
│  ├─ fileDisplay.js
│  └─ useHandleStreamResponse.js
